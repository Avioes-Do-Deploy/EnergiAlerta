"use client"
import Link from "next/link"
import { ArrowLeft, Mail, CheckCircle2 } from "lucide-react"
import { FormEvent, useState } from "react"
export default function RecuperarSenhaPage() { const [sent,setSent]=useState(false); const submit=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setSent(true)}; return <main className="flex min-h-screen items-center justify-center px-6 py-12"><div className="w-full max-w-md animate-reveal-up"><Link href="/login" className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4"/> Voltar ao login</Link><div className="mt-16"><p className="text-sm font-semibold uppercase tracking-[0.18em] text-primary">Acesso seguro</p><h1 className="mt-3 font-display text-4xl font-semibold tracking-tight">Recupere sua senha.</h1><p className="mt-3 leading-7 text-muted-foreground">Enviaremos um link para você criar uma nova senha.</p>{sent?<div className="mt-10 rounded-2xl border border-primary/30 bg-primary/10 p-5"><CheckCircle2 className="size-6 text-primary"/><p className="mt-3 font-semibold">Link enviado</p><p className="mt-1 text-sm text-muted-foreground">Confira sua caixa de entrada para continuar.</p></div>:<form onSubmit={submit} className="mt-10 space-y-5"><label htmlFor="email" className="block text-sm font-medium">E-mail</label><div className="relative"><Mail className="absolute left-3 top-3.5 size-4 text-muted-foreground"/><input required id="email" name="email" type="email" defaultValue="teste@energialerta.com" className="h-12 w-full rounded-xl border border-input bg-background pl-10 pr-4 outline-none ring-primary focus:ring-2"/></div><button className="w-full rounded-xl bg-primary py-3.5 text-sm font-bold text-primary-foreground transition hover:scale-[1.01]">Enviar link de recuperação</button></form>}</div></div></main> }
